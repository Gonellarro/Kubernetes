apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dbvolume-pvc
  namespace: backend
spec:
  resources:
    requests:
      storage: 1000Mi
  accessModes:
    - ReadWriteOnce
  storageClassName: ""
